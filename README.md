# Football Championship Management System ⚽🏆

Комплексная система для управления футбольными чемпионатами. Позволяет вести учет команд, игроков, стадионов, планировать матчи, рассчитывать стоимость билетов и формировать турнирные таблицы.

## 🚀 Стек технологий

### Backend
- **Framework:** FastAPI (Python 3.11)
- **ORM:** SQLAlchemy (PostgreSQL)
- **Validation:** Pydantic v2
- **Auth:** JWT (Jose), Passlib (bcrypt)
- **Docs:** Swagger UI & ReDoc (автоматическая генерация)

### Frontend
- **Framework:** React + Vite
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **Icons:** Lucide React / Heroicons

### Infrastructure
- **Containerization:** Docker, Docker Compose
- **Database:** PostgreSQL 15
- **Proxy:** Nginx (для фронтенда в Docker)

## ✨ Основные возможности
- **Управление пользователями:** Регистрация, логин и роли (Админ / Менеджер).
- **Команды и Игроки:** Полный цикл CRUD для управления составами.
- **Стадионы:** Учет площадок и их вместимости.
- **Матчи:** Планирование игр, ввод результатов и автоматическое обновление турнирной таблицы.
- **Билетная система:** Калькулятор стоимости билетов в зависимости от категории и статуса матча.
- **Аудит:** Логирование важных действий пользователей в системе.
- **Отчеты:** Формирование аналитических данных по чемпионату.

## 🛠 Установка и запуск

### Предварительные требования
- Docker и Docker Compose

### Быстрый запуск
1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-username/football_championship_system.git
cd football_championship_system
```
Настройте переменные окружения:

Скопируйте .env.example в backend/.env и заполните его.

Запустите проект через Docker Compose:

```bash
docker-compose up --build
```

Система будет доступна по адресам:

Frontend: http://localhost:8080

Backend API: http://localhost:8000

Swagger UI: http://localhost:8000/docs

📂 Структура проекта

backend/app — Исходный код API (Routers, Services, Repositories, Models).

frontend/src — Клиентское приложение на React.

database/ — Скрипты инициализации (init.sql) и начальные данные (seed.sql).

docker-compose.yml — Конфигурация всей инфраструктуры.

🔒 Безопасность

Для доступа к API используется JWT-токен. Пароли хешируются с использованием bcrypt. Права доступа разграничены на уровне ролей в приложении.
